const userKeys = {
  all: ['users'],
  me: ['users', 'me']
}


const accountKeys = {
  all: ['accounts'],
  create: ['accounts', 'create'],
  update: ['accounts', 'update'],
  delete: ['accounts', 'delete'],
}

const categoryKeys = {
  all: ['category']
}
export {
  accountKeys, categoryKeys, userKeys
}

